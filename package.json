{
    "name": "pm-tests-piggybacks",
    "description": "Piggybacks tests for PM",
    "version": "0.0.1",
    "private": true,
    "type": "module",
    "devDependencies": {
        "@types/node": "^25.8.0",
        "typescript": "~6.0.2",
        "upen": "^1.0.2",
        "vite": "^8.0.12"
    },
    "scripts": {
        "dev": "pnpm -s gen:pages && vite",
        "build": "pnpm -s gen:pages && upen && tsc && vite build",
        "preview": "vite preview",
        
        "to-npm": "pnpm run build && gh-pages -d dist",
        "gen:pages": "node scripts/generate-pages.js"
    }
}
